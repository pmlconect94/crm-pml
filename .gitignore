# Dependencies
node_modules

# Build artifacts
dist
dist-ssr
.vite
vite.config.d.ts
vite.config.js
vite.config.js.map
*.tsbuildinfo

# Local / secret env (ver .env.example para la plantilla)
*.local
.env
.env.local
.env.*.local

# Python (scripts de carga/utilidades)
__pycache__/
*.pyc
.venv/

# sat-sync: nunca subir la e.firma ni datos descargados (solo existen local para pruebas -
# en GitHub Actions esos mismos archivos se recrean desde Secrets en cada corrida)
sat-sync/credentials/
sat-sync/data/

# Editor / OS
.DS_Store
.thumbnail
*.log

# Carpetas locales de trabajo (no son código fuente)
screenshots/
uploads/

# Claude Code session-local artifacts (no son del proyecto)
.claude/.credentials.json
.claude/settings.local.json

.vercel
.env*
